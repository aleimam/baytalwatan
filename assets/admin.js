/* Admin panel — built in the admin stage (backend admin.php + this UI).
   Stub for now so the app loads cleanly; the Admin tab stays hidden for
   non-admins. */
const Admin = {
  user: null,
  async render(){ /* user management + appearance settings render here */ }
};
